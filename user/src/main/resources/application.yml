spring:
  zipkin:
    base-url: http://127.0.0.1:9411/
  application:
    name: USER
  datasource:
    driver-class-name: org.h2.Driver
    username: root
    url: jdbc:h2:~/test1
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: 'true'
        show_sql: 'true'
    hibernate:
      ddl-auto: update


server:
  port: 9002

